# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=1000000
SAVEHIST=1000000
HISTDUP=erase
setopt notify
setopt share_history
setopt inc_append_history
setopt hist_ignore_all_dups
setopt hist_save_no_dups
setopt hist_find_no_dups
unsetopt autocd beep extendedglob nomatch

# Add my fun ctrl +a, etc. (emacs bindings)
bindkey -e

# End of lines configured by zsh-newuser-install
# The following lines were added by compinstall
zstyle :compinstall filename '/home/mask/.zshrc'

# make completion non case specific
autoload -U compinit && compinit
zstyle ':completion:*' matcher-list 'm:{a-z}={A-Za-z}'

autoload -Uz compinit
compinit
# End of lines added by compinstall

source ~/.aliases.sh

# pnpm
export PNPM_HOME="$HOME/.local/share/pnpm"
export PATH="$PNPM_HOME:$PATH"
# pnpm end

eval "$(zoxide init zsh)"
eval "$(starship init zsh)"

# arrowkey + direction
bindkey "^[[1;5C" forward-word
bindkey "^[[1;5D" backward-word

# HUI specific fixes
while [[ $(pwd) = /_/**/hammer-ui/* ]]; do
    echo automatically moving from $(pwd) to $(dirname $(pwd))
    cd ..
done

# bun completions
[ -s "/home/mask/.bun/_bun" ] && source "/home/mask/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

# Add node
eval "$(fnm env --use-on-cd)"

# bun completions
[ -s "/Users/st/.bun/_bun" ] && source "/Users/st/.bun/_bun"

# jira-cli
export JIRA_API_TOKEN='ATATT3xFfGF0ohAmtaAjwNRFYSJS-FoAI3LX5Ep0VOGiyUzG2DuHODzXOThrGOY7ZrOAFCtKzBAEJA2LkZWmhZYBLvGGWz6pduVYWN8VF_g5GCv-A1wARi26GVoAuA-KY2OjVSjciGicYFswrRYTSqO33nvvzh_mDfuTorZHiyvP1q1l0IUFZNQ=99709D6C'

# qmk
export PATH="$HOME/.local/bin:$PATH"

# deno
export DENO_INSTALL="/home/mask/.deno"
export PATH="$DENO_INSTALL/bin:$PATH"
